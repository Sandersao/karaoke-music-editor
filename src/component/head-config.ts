import {LitElement, css, html} from 'lit';

export class HeadConfig extends LitElement {
  static readonly styles = css``;

  render() {
    return html`
      <h3>Tests</h3>
      <p>Tests with a new element</p>
    `
  }
}